import type { HeaderVariant } from "./header-types";

export const HEADER_VARIANTS_CLASSNAMES: Record<NonNullable<HeaderVariant>, string> = {
  default: "header--default",
  floating: "header--floating",
};
